# Azure SQL Database -- copy to .env and fill in. .env is gitignored.
# Server name is on the database's Overview page, e.g. mist460.database.windows.net
AZURE_SQL_SERVER=
AZURE_SQL_DATABASE=
AZURE_SQL_USER=
AZURE_SQL_PASSWORD=
